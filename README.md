# How to use

* `npm install --global gatsby-cli` && `npm install gh-pages --save-dev` – install scripts
* `npm install` – install dependencies
* `gatsby develop` – run locally
* `npm deploy` – deploy to prod
* `git push origin source` – don't forget to push the underlying changes
